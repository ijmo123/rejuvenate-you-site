---
interface Props {
    location?: 'scottsdale' | 'phoenix' | 'both';
    variant?: 'default' | 'outline';
}

const { location = 'both', variant = 'default' } = Astro.props;

const scottsdalUrl = 'https://rejuvenateyouwaxing.glossgenius.com/services';
const phoenixUrl = 'https://app.squareup.com/appointments/book/e9ycxyisf79aby/L96KEQZQVHJ37/start';

const isDefault = variant === 'default';
---

<div class="flex flex-col sm:flex-row gap-4 items-center justify-center">
    {location === 'scottsdale' || location === 'both' ? (
        <a
            href={scottsdalUrl}
            target="_blank"
            rel="noopener noreferrer"
            class={`px-8 py-3 rounded-lg font-bold transition shadow-md hover:shadow-lg ${isDefault ? 'text-white' : 'text-purple-700 border-2 border-purple-700 hover:bg-purple-50'}`}
            style={isDefault ? 'background-color: #53009D;' : ''}
        >
            Book Scottsdale
        </a>
    ) : null}

    {location === 'phoenix' || location === 'both' ? (
        <a
            href={phoenixUrl}
            target="_blank"
            rel="noopener noreferrer"
            class={`px-8 py-3 rounded-lg font-bold transition shadow-md hover:shadow-lg ${isDefault ? 'text-white' : 'text-purple-700 border-2 border-purple-700 hover:bg-purple-50'}`}
            style={isDefault ? 'background-color: #53009D;' : ''}
        >
            Book Phoenix
        </a>
    ) : null}
</div>